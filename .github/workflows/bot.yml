name: Daily Telegram Bot

on:
  schedule:
    - cron: "0 7 * * *"   # هر روز ساعت 7 صبح
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run bot
        env:
          TELEGRAM_TOKEN: ${{ 8335997516:AAFCM2n5-kl3khcesrN4yyfDg8Z_Dwds2T8 }}
          TELEGRAM_CHAT_ID: ${{ @islamic_creed_towhid2025 }}
        run: python bot.py
